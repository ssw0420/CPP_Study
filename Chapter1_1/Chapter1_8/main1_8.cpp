#include <iostream>

using namespace std;

int main()
{
	int x = 2; // x is a variable, 2 is a literal.
	// '=' : assignment

	cout << x + 2 << endl; // binary. Operand : x , 2
	cout << -x << endl; // unary

	cout << "My Home" << endl;

	x = 1;
	int y = (x > 0) ? 1 : 2; // if ? True : False;
}